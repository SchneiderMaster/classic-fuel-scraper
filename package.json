{
  "name": "classic-fuel-scraper",
  "version": "1.0.0",
  "description": "A scraper for classic fuel stations. Exposes the data via a Prometheus endpoint.",
  "license": "ISC",
  "author": "SchneiderMaster",
  "type": "module",
  "main": "./src/main.ts",
  "scripts": {
    "app": "tsc && node --enable-source-maps --experimental-print-required-tla dist/main.js ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.14.0",
    "cheerio": "^1.2.0",
    "dotenv": "^17.4.1",
    "http": "^0.0.1-security",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^6.0.2"
  }
}
